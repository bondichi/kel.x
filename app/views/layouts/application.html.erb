<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>KelX</title>
  </head>

  <body>
    <!-- This is the generic wrapper for the entire body including header, page content and footer -->
    <div class="generic-page-wrapper">
      <!-- This is the generic wrapper for header and page content Excludes footer-->
      <div class="generic-page-wrapper-content">


    <!-- navbar -->
    <!--     this if statement is for not showing navbar in survey page -->
      <% if content_for?(:navbar) %>
        <%= yield(:navbar) %>
      <% else %>
        <%= render 'shared/navbar' %>
      <% end %>
      <!-- This is for alerts -->
      <%= render 'shared/flashes' %>

      <%= yield %>

      <!-- footer -->
      <!-- this if statement is for not showing footer in survey page -->
    </div>
      <!-- This is a generic wrapper which will allow us to target footer and ensure remains fixed on all pages -->
      <div class="generic-page-wrapper-footer">
        <% if content_for?(:footer) %>
          <%= yield(:footer) %>
        <% else %>
          <%= render 'shared/footer' %>
        <% end %>
      </div>
    </div>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= favicon_link_tag "logo.png" %>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag "application" %>
  </body>
</html>
